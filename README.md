# Сериализация двусвязного списка с произвольными ссылками

В рамках данной работы реализованы сериализация и десериализация двусвязного списка, узлы которого содержат указатели на предыдущий и следующий элементы, а также произвольный указатель на любой узел списка. Структура узла задана фиксированно и не подлежит изменению.

## Cтек

C++20, cmake 3.8.

## Использование

Команда реализует сериализацию структуры данных, заданной в inlet.in:
```
./serializer
```
Команда реализует десериализацию сериализованной структуры данных в outlet.out:
```
./serializer deserialize
```
Для запуска тестов можно запустить
```
./serializer_tests
./parser_tests
```

## Сборка

Для сборки проекта используется CMake:

```
mkdir build && cd build
cmake ..
make
```

## Примечание
Для тестирования использовалась собственная простая header-only реализация системы, позволяющая ограничиться использованием только стандартной библиотеки C++.